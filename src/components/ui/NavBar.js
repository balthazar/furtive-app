'use strict';

import NavBar from 'react-native-navbar';

import colors from '../colors';

export default (
  <NavBar title='Furtive'
    backgroundStyle={{backgroundColor:colors.base02}}
    titleColor='white'/>
);
